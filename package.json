{
  "private": true,
  "scripts": {
    "build": "pnpm --filter pkg-c run build",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "lua-types": "^2.13.1",
    "typescript": "~5.9.0",
    "typescript-to-lua": "^1.33.2"
  }
}
