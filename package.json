{
  "private": true,
  "scripts": {
    "build": "pnpm --filter pkg-c run build",
    "clean": "pnpm -r run clean",
    "e2e:engine-api": "pnpm --filter e2e-engine-api run run"
  },
  "devDependencies": {
    "lua-types": "^2.13.1",
    "typescript": "~5.9.0",
    "typescript-to-lua": "^1.33.2"
  }
}
